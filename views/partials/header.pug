nav(class="navigation-bar")
    p OpenForum
    a(href="/" id="main-page") Главная
    a(href="/topics" id="forum-page") Тематические разделы

    if !authorised
        a(href="/login" id="chat-page") Чаты
        div(class="user-card")
            img(src="/images/profile.png" class="avatar")
            a(href="/login") Войти
    else
        a(href="/chats" id="chat-page") Чаты
        div(class="user-card")
            img(src="/images/profile.png" class="avatar")
            a(href="/users/-1/profile") #{username}

